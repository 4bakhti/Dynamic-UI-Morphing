{
  "name": "dynamic-ui-morphing-framework",
  "version": "0.1.0",
  "private": true,
  "description": "Behaviour Agent: privacy-first numeric telemetry engine that drives real-time UI morphing",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "typescript": "^7.0.2",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
